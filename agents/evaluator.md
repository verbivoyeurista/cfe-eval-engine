# Evaluator

Guidelines for running evaluations on gathered data and producing outputs to `data/evaluated/`.

- Use `library/scoring.py` to compute scores.
- Output: JSON files containing original record plus `score` and `confidence` fields.
