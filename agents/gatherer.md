# Gatherer

Describe how data should be gathered and placed into `data/gathered/`.

- Expected file types: JSON files containing lists or records.
- Naming convention: `source-YYYYMMDD-<id>.json`
- Minimal fields: `id`, `timestamp`, and `value` or domain-specific fields.
